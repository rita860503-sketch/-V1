# 瑪雅密碼計算器（Cosmic Lite）— Scheme A

**v1.3.3-lite (Scheme A)**
- 再次修正「日期不出現」：除了監聽 `input/change/click/blur`，加上 0.5s 輕量輪詢檢查（某些 Safari/嵌入環境事件不觸發的保險機制）。
- 其餘演算法與 UI 與先前版本相同。

## 部署
上傳 `index.html` 到 repo 根目錄 → Settings → Pages → Deploy from a branch（main / root）。

Build 2025-11-16 11:29
