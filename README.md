# 瑪雅密碼計算器（Cosmic Lite）— Scheme A

**修正 v1.3.2-lite (Scheme A)**：修復 Safari/行動版瀏覽器在「月份/年份選擇後，日期不顯示」的問題（改為同時監聽 `input` 與 `change` 事件）。

## 部署
將 `index.html` 與 `README.md` 放在 repo 根目錄 → Settings → Pages → Deploy from a branch（main / root）。

Build 2025-11-16 11:12
